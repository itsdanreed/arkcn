# Fieldset

Group related Field.Root controls inside Fieldset.Root. Legend names the group; HelperText and ErrorText describe it. Use disabled on the root to disable nested controls and invalid to show the group's error state.

The root renders a fieldset. Prefer the native fieldset and legend semantics when composing custom elements. RootProvider accepts the state returned by useFieldset.
