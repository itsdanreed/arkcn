# Portal

Portal.Root renders children outside their parent DOM container, normally at the document body. A container ref selects another destination, and disabled renders children in place.

Portaling does not add modal semantics, focus management, or a backdrop. Use Dialog or another overlay component when those behaviors are needed. Portal adds no DOM wrapper of its own.
