# Download Trigger

DownloadTrigger.Root accepts data, fileName, and mimeType. Data can be text, a Blob, a File, or a function returning downloadable data. Use asChild to attach the download behavior to a custom button.

The example downloads a small local text file. useDownload exposes the behavior independently of the trigger.
