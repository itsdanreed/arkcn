# Client Only

ClientOnly.Root renders its fallback during server rendering and until the client mounts. Its children can be content or a function evaluated on the client. It does not introduce a DOM wrapper.
