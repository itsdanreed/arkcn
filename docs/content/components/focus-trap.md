# Focus Trap

FocusTrap.Root keeps keyboard focus within its children while enabled. Set disabled to release it. Use initialFocus, fallbackFocus, and returnFocusOnDeactivate to control focus entry and restoration.

Always provide an accessible way to leave the trapped region. The example's Release focus button disables the trap. The root supports asChild.
