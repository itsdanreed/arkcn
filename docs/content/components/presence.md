# Presence

Presence.Root coordinates mounting with enter and exit animations. Set present to show content and unmountOnExit to remove it after the exit completes. Style its state attributes with animation classes.

The root supports asChild. usePresence and Presence.Provider expose the lifecycle independently when a custom composition needs it.
